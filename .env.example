# Copy this to .env (gitignored) and fill in your inference.sh API key.
# Easier alternative: `belt login --key <KEY>` writes ~/.inferencesh/config.json,
# which skillos will read automatically.

INFSH_API_KEY=
